extends Node2D

#onready var area_two_d = $Area2D
# Declare member variables here. Examples:
# var a = 2
# var b = "text"


# Called when the node enters the scene tree for the first time.
#func _ready():
#	area_two_d = get_node("/root/Main/PlayerHearing/")


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass

#func _physics_process(delta):
#	if area_two_d.overlaps_area(get_node("/root/Main/Player/SightZoneLight")):
#		self.queue_free()
